<p id="notice"><%= notice %></p>

<p>
  <strong>Nome:</strong>
  <%= @creditor.name %>
</p>

<%= link_to 'Edit', edit_creditor_path(@creditor), class: 'btn' %> |
<%= link_to t('back'), creditors_path, class: 'btn' %>
